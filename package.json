{
  "name": "climate-risk-calculator-root",
  "version": "1.0.2",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}